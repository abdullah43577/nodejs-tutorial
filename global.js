// console.log(global);

// global.setTimeout(() => {
//   console.log('in the timeout');
// }, 3000);

// setTimeout(() => {
//   console.log('in the timeout');
//   clearInterval(int);
// }, 3000);

// const int = setInterval(() => {
//   console.log('in the interval');
// }, 1000);

// console.log(__dirname); // gets the absolute path of the current folder
// console.log(__filename); // gets the absolute path of the current folder with the filename added as well
